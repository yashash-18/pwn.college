level-1:  > Run the /challenge/server in the terminal in desktop.
          > opened the fire fox and pasted the url that we got from the before step.

          Flag: pwn.college{ktfvMhfzJlCVIrpGS0XtD9enN7O.0lMxgDNxwyN1YTN0EzW} 

level-2:  > Run the /challenge/server in the terminal in desktop. 
          > cat /challenge/server to know what the code present inside it.
          > get the endpoint from that. 
          > opened the fire fox and pasted the url along with endpoint that we got from the before step.

          Flag: pwn.college{AtpWyq1ktQ5_wrOg_KjVa6el26q.01MxgDNxwyN1YTN0EzW}

level-3:  > Run the /challenge/server in the terminal in desktop.
          > cat /challenge/server to know what the code present inside it.
          > opened the fire fox and pasted the url along with endpoint that we got from the before step.
          > go to menu and then open more tools and click on page source we get the hidden flag.

          Flag: pwn.college{IRqkj7eCTvfg7PjAswXGOivkvSp.oFNxgDNxwyNlYTNOEZw}

level-4:  > Run the /challenge/server in the terminal in desktop.
          > cat /challenge/server to know what the code present inside it.
          > opened the fire fox and pasted the url along with endpoint that we got from the before step.
          > go to menu and then open more tools and click on web developer tools and click on network tab and reload the page and go to headers tab and in that under response header we get the hidden flag.

          Flag: pwn.college{oDTA87wc01enXg9QmqQoRCRIIFd.0VNxgDNxwyN1YTN0EzW}

level-5:  > Run the /challenge/server in the terminal .
          > open another terminal and run nc challenge.localhost 80 , since end point is given.
          > run Get /path HTTP/1.1  (there mention which path it should go)
          > get the flag.

          Flag: pwn.college{gvYuEL5MHFB6lOLW63zE80s_yGU.QX5YjMzwYN1YTN0EzW}

level-6:  > Run the /challenge/server in the terminal .
          > cat /challenge/server and find the endpoint .  
          > open another terminal and run nc challenge.localhost 80
          > run Get /path HTTP/1.1  (there mention which path it should go)
          > get the flag.

          Flag: pwn.college{0HeUd8t3ZvvslAfrWNIdcE6rJqs.QX1cjMzwyN1YTN0EzW}

level-7:  > Run the /challenge/server in the terminal .
          > cat /challenge/server and find the endpoint . 
          > open another terminal and run curl http://challenge.localhost/endpoint  (using get request we run curl in this way ,we get endpoint from the before step)
          > get the flag.

          Flag: pwn.college{0zwaKRxHgklOhnxR7Ka4Y6_3jHK.QX0cjMzwyN1YTN0EzW}

level-8:  > Run the /challenge/server in the terminal .
          > cat /challenge/server and find the endpoint .
          > open another terminal and run python to open the pythone notebook.
          > import requests , assign a variable name to requests.get("http://challenge.localhost/endpoint"), and then print(variablename.url or variablename.text) url for link and text for the web content.
          > get the flag

          Flag: pwn.college{UZcpFAvHob506rcwQBg3aEdo4It.QX2cjMzwyN1YTN0EzW}

level-9:  > Run the /challenge/server in the terminal .
          > cat /challenge/server and find the endpoint and also check for new host..
          > open another terminal and run python to open the pythone notebook.
          > import requests , assign a variable name to requests.get("http://challenge.localhost/endpoint",headers={"Host":"newhostname"}), and then print(variablename.text)
          > get the flag

          Flag: pwn.college{gphZ0uzs3rWUZn2Rc0TA1nowe8L.QXzcjMzwyN1YTN0EzW}

level-10:  > Run the /challenge/server in the terminal .
           > cat /challenge/server and find the endpoint and also check for new host..
           > open another terminal and run curl -H "Host: newhostname" http://challenge.localhost/endpoint ( it is the syntax for curl in setting the host header)
           > get the flag

          Flag: pwn.college{wOOtEhrGoq2mpb735GzsW740oY2.QXxcjMzwyN1YTN0EzW}

level-11:  > Run the /challenge/server in the terminal .
           > cat /challenge/server and find the endpoint and also check for new host..
           > open another terminal and run nc challenge.localhost 80
           > run Get /endpoint HTTP/1.1
           > Host: newhostname
           > press enter twice after the last line
           > get the flag

          Flag: pwn.college{AQ82RHH4JM1W0_7Xm7nXhkuESBi.QXycjMzwyN1YTN0EzW}

level-12:  > Run the /challenge/server in the terminal .
           > cat /challenge/server and find the endpoint and also check for new host..
           > open another terminal and run nc challenge.localhost 80
           > run Get /endpoint with encoded HTTP/1.1
           > press enter twice after the last line
           > get the flag

          Flag: pwn.college{IO6VrRH3OUZdWACJhZS_GyO97oV.QX5cjMzwyN1YTN0EzW}

level-13:  > Run the /challenge/server in the terminal .
           > cat /challenge/server and find the endpoint and also check for the parameters.
           > open another terminal and run curl "http://challenge.localhost:80/solve?password=dpiocwev"
           > get the flag

          Flag: pwn.college{MK45pCiqdtEtRJWesAJCSZdz352.QXwgjMzwyN1YTN0EzW}

level-14:  > Run the /challenge/server in the terminal.
           > cat /challenge/server and find the endpoint and also check for the parameters.
           > open another terminal and run nc challenge.localhost 80
           > run Get /endpoint?parameter1=&parameter2= HTTP/1.1
           > run Host: challenge.localhost
           > press enter twice after that.
           > get the flag.

          Flag: pwn.college{EwC6tnLCMgJbVVohGxUD__lxt64.QX0gjMzwyN1YTN0EzW}

level-15:  > Run the /challenge/server in the terminal.
           > cat /challenge/server and find the endpoint and also check for the parameters.
           > open another terminal and run curl "http://challenge.localhost:80/hack?parameter1=&parameter2="
           > press enter twice after that.
           > get the flag.

          Flag: pwn.college{cyyJA2rfPwqLem0aPYLYGIt_0Wj.QXzgjMzwyN1YTN0EzW}

level-16:  > Run the /challenge/server in the terminal in desktop.
           > cat /challenge/server and find the endpoint.
           > open browser and run the url
           > a form will be visible and enter the passowrd there
           > get the flag.

          Flag: pwn.college{oiY3N78OxDj-iQa0iyaSrOz98iq.0lNxgDNxwyN1YTN0EzW}

level-17:  > Run the /challenge/server in the terminal . 
           > cat /challenge/server and find the endpoint.
           > open another terminal and run curl -X POST -d "key=password" http://challenge.localhost:80/challenge
           > get the flag.

          Flag: pwn.college{o5jyeO9g7F6YlLzjocPT6x28EbG.QX2gjMzwyN1YTN0EzW}

level-18:  > Run the /challenge/server in the terminal 
           > cat /challenge/server and find the endpoint.
           > open another terminal and run nc challenge.localhost 80
           > run POST /endpoint HTTP/1.1
           > run host: host-header
           > run content-type: application/x-www-form-urlencoded
           > run content-length: length of the string
           > enter empty line so that it will understand that headers are completed now body will start.
           > enter the body
           > get the flag.

          Flag: pwn.college{0nxl5N4Tx-rBbm3CWgveeAQg2FD.QX3gjMzwyN1YTN0EzW}

level-19:  > Run the /challenge/server in the terminal.
           > cat /challenge/server and find the endpoint.
           > open another terminal and run python so that it opens python notebook
           > run import requests
           > revise="http://challenge.localhost:80/endpoint"
           > params={"pin" : "password"}
           > response=requests.post(revise,data=params)  [ here data is an in-built parameter of requets.post() ]
           > print(response.text)
           > get the flag.

          Flag: pwn.college{g2J0EuvdYJ6VFdtuO2JqoIokrNX.QX4gjMzwyN1YTN0EzW}

level-21:  > Run the /challenge/server in the terminal
           > cat /challenge/server and find the endpoint
           > open another terminal and run curl -X POST -d "code=pass" -d "flag=pass" -d "signature=pass" http://challenge.localhost:80/endpoint
           > get the flag

          Flag: pwn.college{kZLXF7WhF0Iiqtv8HGHebx6cODp.QX5gjMzwyN1YTN0EzW}

level-22:  > Run the /challenge/server in the terminal.
           > cat /challenge/server and find the endpoint
           > open another terminal and run nc challenge.localhost 80
           > run POST /submit HTTP/1.1
           > host: challenge.localhost
           > content-type: application/x-www-form-urlencoded
           > content-length: 68
           > enter a empty line so that the server can understand the headers are completed and the body starts 
           > name1=pass1&name2=pass2&name3=pass3
           > get the flag.

          Flag: pwn.college{wpFtMk4ltopFaPfPj_EMXckKeez.QXwkjMzwyN1YTN0EzW}

level-23: > Run the /challenge/server in the terminal
          > cat /challenge/server and find the  1st endpoint
          > open another terminal and run nc challenge.localhost 80
          > run Get /endpoint1 HTTP/1.1
          > host: host-header
          > u will get a new endpoint in that page.
          > now run nc challenge.localhost 80
          > Get /endpoint2 HTTP/1.1
          > get the flag

          Flag: pwn.college{UR4mpTI6_G7WJpxmXIiMtTSV-hi.QX5kjMzwyN1YTN0EzW}

level-24:  > Run the /challenge/server in the terminal
           > cat /challenge/server and find the  1st endpoint
           > run curl -L http://challenge.localhost:80/endpoint  ( here -L refers to which location should it go)
           > u will get the flag directly since curl redirects directly and gives the final result.

          Flag: pwn.college{QgjRirq1uDnUZQaXMfHfJud0zGi.QX4kjMzwyN1YTN0EzW}

level-25:  > Run the /challenge/server in the terminal




